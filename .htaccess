<IfModule mod_rewrite.c>

Options -Indexes
ErrorDocument 403 “Akses folder ini tidak diperbolehkan.”

RewriteEngine On

# RewriteBase

RewriteCond %{REQUEST_URI} !^/public
RewriteRule ^(.\*)$ public/$1 [L]

</IfModule>
